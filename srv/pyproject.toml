[project]
name = "srv"
version = "0.1.0"
description = "Sink packet viewer — receives Packet payloads from packer.ts"
requires-python = ">=3.12"
dependencies = ["flask>=3.0"]

[project.scripts]
srv = "main:run"

[dependency-groups]
dev = [
    "ruff>=0.15.4",
    "ty>=0.0.19",
]
